import {Container} from 'reactstrap';
import styled from 'styled-components';

export const CampaignListStyled = styled.div`
  position: relative;
  min-height: 400px;
`;

export const CampaignContainerStyled = styled(Container)`
  position: relative;
  min-height: 400px;
`;
