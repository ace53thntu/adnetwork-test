import {Button} from 'reactstrap';
import styledComponents from 'styled-components';

export const ClearSearchButton = styledComponents(Button)`
  position: absolute;
  right: 50px;
  top: 50%;
  transform: translateY(-50%);
`;
