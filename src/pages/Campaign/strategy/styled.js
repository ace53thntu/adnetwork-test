import {Container} from 'reactstrap';
import styled from 'styled-components';

export const StrategyListStyled = styled.div`
  position: relative;
  min-height: 400px;
`;

export const StrategyContainerStyled = styled(Container)`
  position: relative;
  min-height: 400px;
`;
